#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print 0 to 9
 *
 * Return: Always success
 */
int main(void)
{
	int n;
	char c;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	for (c = 'a'; c <= 'f'; lette++)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}

